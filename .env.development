# Local dev only (Vite loads this in `vite dev`, never in production builds).
# Forces dashboard "app mode" so /onboard, /dashboard, etc. are reachable on
# localhost — without it every route falls back to the marketing Landing page.
VITE_IS_APP=1
